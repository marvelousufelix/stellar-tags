datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model username_registry {
  username   String @id
  address    String @unique
  created_at String

  @@index([username])
}
